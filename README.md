# Serenity Sound

Serenity Sound 是一款专为放松、睡眠和专注设计的极简主义环境音效混音应用。它允许用户混合多种高品质的自然声音，创造个性化的听觉环境，并提供复古风格的视觉体验。

## 🌟 核心功能

- **多音轨混合 (Analog Mixer)**：支持同时播放雨声、森林、海浪、冥想等多种环境音。
- **独立音量控制**：每个音效都可以独立调节音量，打造完美的平衡。
- **自定义场景 (Scenes)**：将你最喜欢的混音配置保存为“场景”，一键快速切换。
- **睡眠定时器 (Sleep Timer)**：设置倒计时，在入睡后自动停止播放。
- **复古视觉设计**：
  - 复古数字时钟/定时器显示屏。
  - 动态“呼吸灯”音效图标，实时反馈播放状态。
  - 沉浸式暗黑渐变主题。
- **后台播放支持**：集成系统音频服务，支持锁屏控制和后台持续播放。
- **数据持久化**：自动保存您的音效排序、音量设置和自定义场景。

## 🛠️ 技术栈

- **框架**: [Flutter](https://flutter.dev/) (Multi-platform)
- **状态管理**: [Flutter Riverpod](https://riverpod.dev/)
- **音频引擎**: [just_audio](https://pub.dev/packages/just_audio) & [audio_service](https://pub.dev/packages/audio_service)
- **本地存储**: [Hive](https://pub.dev/packages/hive) (高性能 NoSQL 数据库)
- **矢量图形**: [flutter_svg](https://pub.dev/packages/flutter_svg)
- **依赖注入**: Riverpod Providers

## 📂 项目结构

```text
lib/
├── main.dart             # 应用入口、主屏幕 UI 及生命周期管理
├── models/
│   └── sound_effect.dart # 数据模型 (SoundEffect, SoundScene)
├── providers/
│   └── sound_provider.dart # 业务逻辑与状态管理 (音效列表、激活状态、定时器)
├── services/
│   ├── audio_handler.dart  # 后台音频控制逻辑
│   └── storage_service.dart # Hive 本地存储初始化与服务
├── widgets/
│   └── breathing_logo.dart # 自定义动画组件 (呼吸灯效果)
assets/
├── audio/                # 高品质 AAC 音频资源
├── svg/                  # 极简风格图标
└── config/
    └── sounds.json       # 音效元数据配置文件
```

## 🚀 快速开始

### 前置要求

- Flutter SDK (推荐最新稳定版)
- Dart SDK
- Android Studio / Xcode

### 安装步骤

1. **克隆项目**
   ```bash
   git clone <repository-url>
   cd serenity_sound
   ```

2. **获取依赖**
   ```bash
   flutter pub get
   ```

3. **生成代码 (Hive TypeAdapters)**
   ```bash
   flutter pub run build_runner build
   ```

4. **运行应用**
   ```bash
   flutter run
   ```

## 📝 配置文件说明

音效的添加和修改可以通过编辑 `assets/config/sounds.json` 实现：

```json
{
  "id": "rain",
  "name": "Rain",
  "svgPath": "assets/svg/rain.svg",
  "audioPath": "assets/audio/rain.aac",
  "themeColor": "#4A90E2"
}
```

## 📄 开源协议

本项目采用 MIT 协议。
